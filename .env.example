# 旧版（bash）配置示例。新版推荐用 `ap setup` 一键写入 ~/.config/ancient-pilot/。
# 如果你仍想用环境变量，它们的优先级高于配置文件。

export AP_API_URL="https://api.deepseek.com/v1/chat/completions"
export AP_MODEL="deepseek-chat"   # 或 deepseek-reasoner
export AP_API_KEY="sk-xxx"
export AP_TIMEOUT=30
